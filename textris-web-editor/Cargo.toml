[package]
name = "textris-web-editor"
version = "0.1.0"
license = "MIT"
edition = "2024"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
textris-pdf = { path = ".." }
wasm-bindgen = "0.2"
console_error_panic_hook = "0.1"
